-- Liste o código dos departamentos dos empregados
SELECT Departamento_Codigo 
FROM Empregado;

-- Liste o código, sem repetição, dos departamentos dos empregados
SELECT DISTINCT(Departamento_Codigo) 
FROM Empregado;
